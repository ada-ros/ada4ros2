# Common prefix where built things end up (not installed, but for using)

set(ADA_BUILD_DIR "@ADA_BUILD_DIR@")
set(ADA_GPR_DIR "@ADA_GPR_DIR@")
set(ADA_RESOURCE_DIR "@ADA_RESOURCE_DIR@")

# MACROS FOR ADA PROJECTS
include(@PROJECT_BINARY_DIR@/ada_project.cmake)

## No idea what this means:
# Our library dependencies (contains definitions for IMPORTED targets)
# include("${rclada_DIR}/export_rclada.cmake")

# These are IMPORTED targets created by rcladaTargets.cmake


