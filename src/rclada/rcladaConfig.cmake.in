# - Config file for the rclada package
# It defines the following variables
#  rclada_INCLUDE_DIRS - include directories for FooBar
#  rclada_LIBRARIES    - libraries to link against
#  rclada_EXECUTABLE   - the bar executable

set(rclada_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${rclada_DIR}/export_rclada.cmake")

# These are IMPORTED targets created by rcladaTargets.cmake


